return {
  "CopilotC-Nvim/CopilotChat.nvim",
  -- opts = function(_, opts)
  --   -- Ensure the prompts table exists
  --   opts.prompts = opts.prompts or {}
  --   -- Override the Docs prompt
  --   opts.prompts.Docs = {
  --     prompt = "/COPILOT_GENERATE Please add documentation comment for the selection without lines number.",
  --   }
  -- end,
}
